[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="synthetic-preference-dpo-pipeline"
version="0.1.0"
requires-python=">=3.10"
dependencies=[]
[project.optional-dependencies]
dev=["pytest>=8"]
train=["torch>=2.2","transformers>=4.40","datasets>=2.18","trl>=0.9","accelerate>=0.30"]
[tool.setuptools.packages.find]
where=["src"]
